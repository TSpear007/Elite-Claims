import { PLANS } from "@/lib/plans";
const money=(c:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(c/100);
export default function RevenuePage(){return <main><div className="eyebrow">Revenue</div><h1>Monetization console</h1><p className="muted">Track lawful recovery fees and recurring platform subscriptions separately. Finder/recovery fees must be configured only after jurisdiction-specific review.</p><div className="grid"><div className="card"><span className="muted">Owner / Super Admin</span><div className="metric gold">$0</div><span className="badge">Permanent</span></div><div className="card"><span className="muted">Starter</span><div className="metric">{money(PLANS.STARTER.monthlyPriceCents)}</div><span className="badge">per month</span></div><div className="card"><span className="muted">Professional</span><div className="metric">{money(PLANS.PRO.monthlyPriceCents)}</div><span className="badge">per month</span></div><div className="card"><span className="muted">Agency</span><div className="metric">{money(PLANS.AGENCY.monthlyPriceCents)}</div><span className="badge">per month</span></div></div><section className="panel"><h2>Revenue controls</h2><div className="modules"><div className="module"><strong>Recovery fees</strong><span className="muted">Case-level fee percentage and expected fee fields are available, but fee terms require compliance approval.</span></div><div className="module"><strong>Subscriptions</strong><span className="muted">Plan catalog is ready for a payment-provider integration.</span></div><div className="module"><strong>Owner entitlement</strong><span className="muted">The OWNER plan is permanently priced at zero and is not payment-gated.</span></div></div></section></main>}
