'use client';
import {FormEvent,useState} from 'react';
export default function UploadForm(){const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMsg('Uploading…');const form=new FormData(e.currentTarget);const r=await fetch('/api/uploads',{method:'POST',body:form});const j=await r.json();setMsg(r.ok?`Received ${j.fileName}. Next: validation and mapping.`:j.error||'Upload failed');}return <form className="card" onSubmit={submit}><h2>New county list</h2><label>State <input name="state" required maxLength={2}/></label><label>County <input name="county" required/></label><label>Official source URL <input name="sourceUrl" type="url" required/></label><label>List file <input name="file" type="file" accept=".csv,.xls,.xlsx,.pdf" required/></label><button type="submit">Upload list</button><p>{msg}</p></form>}
