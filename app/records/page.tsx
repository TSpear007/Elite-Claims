const columns = ["State", "Claimant", "Case / Reference", "Surplus", "Source", "Status"];
export default function RecordsPage(){return <main><div className="eyebrow">Surplus Records</div><h1>National record pipeline</h1><p className="muted">Import public surplus records, normalize names and addresses, deduplicate entries and convert qualified records into claim cases.</p><div className="grid"><div className="card"><span className="muted">Import mode</span><div className="metric">API</div></div><div className="card"><span className="muted">Deduplication</span><div className="metric gold">SHA-256</div></div><div className="card"><span className="muted">Source rule</span><div className="metric">Verified</div></div><div className="card"><span className="muted">Outreach</span><div className="metric">Locked</div></div></div><section className="panel"><h2>Records workspace</h2><div className="table"><div className="tr head">{columns.map(c=><span key={c}>{c}</span>)}</div><div className="empty">Connect PostgreSQL and import the first verified public source to populate this workspace.</div></div></section><section className="panel"><h2>Import API</h2><p className="muted">Protected server endpoint: <code>/api/records</code>. Requests require the server-side admin API key and a valid source ID. Duplicate fingerprints are updated rather than multiplied.</p></section></main>}
