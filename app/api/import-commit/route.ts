import {NextRequest,NextResponse} from 'next/server';import {normalizeCountyRow} from '../../../lib/importer';export async function POST(req:NextRequest){const b=await req.json();if(!b.sourceId||!Array.isArray(b.rows)||!b.mapping)return NextResponse.json({error:'sourceId, rows and mapping required'},{status:400});const accepted:any[]=[];const rejected:any[]=[];for(const [i,row] of b.rows.entries()){try{const n=normalizeCountyRow(row,b.mapping);accepted.push({...n,amountCents:n.amountCents.toString(),sourceId:b.sourceId,compliance:'REVIEW_REQUIRED'});}catch(e){rejected.push({row:i+1,error:e instanceof Error?e.message:'Invalid row'});}}return NextResponse.json({accepted,rejected,notice:'API returns normalized import payload. Database persistence must run only after authenticated source validation and transaction/audit wiring.'},{status:202});}
