import {NextRequest,NextResponse} from 'next/server';export async function POST(req:NextRequest){const b=await req.json();const active=b.status==='VERIFIED'&&!!b.officialSourceUrl&&!!b.statuteCitation&&!!b.lastVerifiedAt;return NextResponse.json({active,feeQuoteEnabled:active,outreachRuleEligible:active,reason:active?'Verified jurisdiction rule':'Rule requires current official-source verification'});}
