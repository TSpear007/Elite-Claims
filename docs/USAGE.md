# Operator usage

Use `/ai-claims` as the command center, `/upload` for county files, `/rules` for state/county compliance, `/scripts` for transparent claimant conversations, `/records` for leads, `/cases` for workflow and `/revenue` for permitted revenue tracking.

Until production integrations are completed, use this branch for development/research workflow and test data rather than live sensitive claimant data or automated outreach.
