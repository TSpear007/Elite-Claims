# Deployment environment

Required production configuration includes DATABASE_URL, verified OWNER_EMAIL identity bootstrap, authentication secrets/provider, encrypted object-storage credentials, outbound-provider credentials after compliance approval, application base URL and monitoring configuration. Never commit real secrets. Rotate credentials and use the hosting platform's secret manager.
