# Security requirements

Production requires verified authentication, least-privilege RBAC, tenant isolation, TLS, encryption at rest, managed secrets, secure object storage, upload malware scanning, input validation, rate limits, CSRF/session protections as applicable, dependency scanning, backups, monitoring and audit logs. Sensitive claimant documents should use short-lived authorized access rather than public URLs.
